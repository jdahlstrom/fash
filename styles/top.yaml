top:
  # Only unique styles not in upper/*
  casual:
    - basketball
    - gored
    - sports
    - tied-front
    - twist{|-front}

  waist:

  sleeved:
    - __fash/styles/upper/casual__
    - __fash/styles/upper/top-blouse-tunic__
    - __fash/styles/upper/top-blouse-tunic-hem__-hem
    - __fash/styles/upper/neckline/sleeved__

  back:
    - back-closure
    - back-lacing
    - backless {|halter}
    - bowtie-back
    - crossback
    - (cutout:0.9)-back
    - deep v-back
    - draped-back
    - gathered-back
    - high-back
    - keyhole-back
    - lace-back
    - mesh-back
    - open-back {|halter}
    - plunge-back
    - scoop-back
    - split-back
    - square-back
    - strap-back
    - racerback
    - tied-back {|halter}
    - v-back
    - x-back
    - y-back
    - zip-back

  # TODO Try to get rid of this in favor of upper/neckline/*
  neckline:
    # Sleeveless, strapless, tube-like.
    bandeau:
      - __fash/**/000-tube-like/bandeau__ bandeau

    # Sleeveless, strapless, tube-like.
    tube:
      - __fash/**/000-tube-like/tube__ tube
