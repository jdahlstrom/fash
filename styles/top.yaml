top:
  # Only unique styles not in upper/*
  casual:
    - basketball
    - gored
    - sports
    - tied-front
    - twist{|-front}

  waist:

  sleeved:
    - __fash/styles/upper/casual__
    - __fash/styles/upper/top-blouse-tunic__
    - __fash/styles/upper/top-blouse-tunic-hem__-hem
    - __fash/styles/upper/neckline/sleeved__

  # TODO Try to get rid of this in favor of upper/neckline/*
  neckline:
    # Sleeveless, strapless, tube-like.
    bandeau:
      - __fash/**/000-tube-like/bandeau__ bandeau

    # Sleeveless, strapless, tube-like.
    tube:
      - __fash/**/000-tube-like/tube__ tube

  # TODO Not yet used, some of these probably won't work well
  back:
    - back-closure
    - back-lacing
    - backless
    - bowtie back
    - button-back
    - crossback
    - criss-cross {|string} back
    - (cutout:0.9) back
    - deep v-back
    - draped back
    - halter back
    - high back
    - keyhole back
    #- '{criss-cross|popover|string} keyhole back'
    #- lace-up back
    #- lace-up open-back
    - mesh back
    - open-back #{|popover} {|closure}
    - open-back halter
    - plunging back
    - scoop-back
    - split-back #{|popover} {|closure}
    - strap-back
    #- string-back
    - racerback
    - tied-back halter
    - v-back
    - y-back
    - zip back
